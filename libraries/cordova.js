/**
 * cordova.js — PLACEHOLDER FILE
 * -----------------------------------------------------------------------
 * Do not edit or replace this file by hand. When you run:
 *
 *     cordova platform add android
 *     cordova prepare android
 *
 * the Cordova CLI automatically generates the real `cordova.js` bridge
 * script (and injects it into the built app's HTML) inside:
 *
 *     platforms/android/app/src/main/assets/www/cordova.js
 *
 * This project references `cordova.js` so that native plugins
 * (camera, file, permissions) become available once built as an APK —
 * see README.md for the exact build steps. In a browser (or when simply
 * testing index.html/login.html/home.html via a local dev server), this
 * file is not required: the app already works fully offline using only
 * standard Web APIs (IndexedDB, getUserMedia, BarcodeDetector).
 * -----------------------------------------------------------------------
 */
